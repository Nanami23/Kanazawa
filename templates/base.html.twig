<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
               and uncomment the following Encore helpers to start using Symfony UX #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous" />
        <link rel="preconnect" href="https://fonts.gstatic.com" />
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css2?family=Hind:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
        <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
        {% block stylesheets %}

        {% endblock %}

        <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <header class="fixed-top">
            <nav class="nav navbar px-5 py-2">
                <a href="{{ path('default') }}" class="navbar-brand">
                    <img src="{{ asset('images/logo2_blanc.svg') }}" alt="Logo Kanazawa" />
                </a>
                <div>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a href="{{ path('user_profil') }}">{{ app.user.firstName }}</a>
                        <a href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                        <a href="{{ path('app_login') }}">Connexion</a>
                        <a href="#">Inscription</a>
                    {% endif %}
                </div>
            </nav>
        </header>
        <div style="height: 52px"></div>
        <main class="px-6 py-5">
            {% block body %}

            {% endblock %}
        </main>
        <footer class="d-flex justify-content-between px-6 py-4">
            <div id="liens" class="d-flex flex-column justify-content-between">
                <a href="{{ path('admin') }}">Administration</a>
                <a href="#">Mentions Légales</a>
                <a href="#">Conditions d'Utilisation</a>
            </div>
            <div class="text-center">
                <div>
                    <p>En cas de besoin, vous pouvez nous contacter via cette adresse mail :
                        <br />
                        <a href="mailto:contact@beyondmemories.fr">contact@beyondmemories.fr</a>
                    </p>
                </div>
                <div id="copyright">
                    <p>Copyright © 2021 | Beyond Memories</p>
                </div>
            </div>
            <div id="réseaux" class="d-flex justify-content-between align-items-center">
                <a href="#"><img src="{{ asset('images/facebook.svg') }}" alt="Logo Facebook"></a>
                <a href="#"><img src="{{ asset('images/instagram.svg') }}" alt="Logo Instagram"></a>
                <a href="#"><img src="{{ asset('images/twitter.svg') }}" alt="Logo Twitter"></a>
            </div>
        </footer>
    </body>
</html>